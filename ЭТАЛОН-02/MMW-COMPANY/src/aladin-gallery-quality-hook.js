// ALADIN Factory gallery overrides are intentionally disabled.
// The canonical ALADIN page now contains the approved gallery selections directly.
// Keeping this module as a no-op prevents runtime rewrites from masking source changes.
module.exports={};
