name: Build current-state project PDFs

on:
  push:
    paths:
      - 'FACTORY-PROJECT-TECHNICAL-CURRENT-STATE-2026-09-08/**'
      - '.github/workflows/build-current-state-pdfs.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-pdfs:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install ReportLab
        run: python -m pip install reportlab

      - name: Generate PDFs
        working-directory: FACTORY-PROJECT-TECHNICAL-CURRENT-STATE-2026-09-08
        run: python generate_current_state_pdfs.py

      - name: Commit generated PDFs
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add FACTORY-PROJECT-TECHNICAL-CURRENT-STATE-2026-09-08/PDF
          git diff --cached --quiet || git commit -m 'FACTORY: generate current-state project PDFs'
          git push
